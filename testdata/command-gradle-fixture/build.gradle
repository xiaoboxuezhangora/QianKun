plugins {
    id 'org.springframework.boot' version '3.2.0'
    id 'java'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

task printVersion {
    doLast {
        println version
    }
}

tasks.register('syncConfig') {
    doLast {
        println 'sync config'
    }
}
